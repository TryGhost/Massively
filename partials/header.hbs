{{!-- Intro --}}
{{#if home}}
    <div id="intro">
        <h1>{{@site.title}}</h1>
        <h1>廣布山</h1>
        <h1>開妙院</h1>
        <p>{{@site.description}}</p>
        <ul class="actions">
            <li><a href="#header" class="button icon solo fa-arrow-down scrolly">Continue</a></li>
        </ul>
    </div>
{{/if}}

{{!-- Header --}}
<header id="header">
    {{#if langText}}
        <a href="{{@site.url}}/{{langCode}}" class="logo" style="font-size: 1rem;">{{langText}}</a>
    {{/if}}
</header>
