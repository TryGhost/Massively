<footer>
    <nav class="pagination" role="navigation">
        {{#if prev}}
        <a class="previous newer-posts" href="{{page_url prev}}">Newer Posts</a>
        {{else}}
        <div class="spacer"></div>
        {{/if}}
        <span class="page">Page {{page}} of {{pages}}</span>
        {{#if next}}
        <a class="next older-posts" href="{{page_url next}}">Older Posts</a>
        {{else}}
        <div class="spacer"></div>
        {{/if}}
    </nav>
</footer>