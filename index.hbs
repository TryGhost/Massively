{{!< default}}

{{> "en" }}

{{#contentFor "header"}}
    {{> header home="active" langCode="zh" langText="中文(简体)"}}

    {{!-- Nav --}}
    {{#if @site.navigation}}
        {{navigation}}
    {{/if}}
{{/contentFor}}

{{> "index" langTag="-hash-zh"}}
